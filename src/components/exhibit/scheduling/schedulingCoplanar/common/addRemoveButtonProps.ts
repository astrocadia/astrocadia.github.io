import { ButtonProps } from '../../../../common/buttons';
import { EditIcon } from '../../../../common/icons';

export const addRemoveButtonProps: ButtonProps = {
  children: 'Add/Remove',
  startIcon: EditIcon({}),
};
