import { FunctionComponent } from 'react';
import { MainPanelHeader } from '../../components/MainPanel';

// Temporary component to indicate sections that still need work

export const ToDo: FunctionComponent = () => <MainPanelHeader title='To Do' />;
